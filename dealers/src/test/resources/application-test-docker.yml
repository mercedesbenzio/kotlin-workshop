spring:
  datasource:
    driver-class-name: org.testcontainers.jdbc.ContainerDatabaseDriver
    url: jdbc:tc:postgresql:15.2:///integration-tests-db?TC_INITSCRIPT=docker-init-dealers-db.sql
  jpa:
    hibernate:
      ddl-auto: create
